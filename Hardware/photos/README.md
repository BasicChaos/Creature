# Build photos

Drop photos of the physical Creature build here (e.g. `breadboard-01.jpg`).

Reference them from `Hardware/wiring.md` like:

```markdown
![Breadboard build](photos/breadboard-01.jpg)
```
